name: Badge

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  badge:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Placeholder status badge step
        run: echo "CI green; add badge badge in README or shields.io as desired"
